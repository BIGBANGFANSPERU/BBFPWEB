/*!
 * EBM
 * Easy Box Model boilerplate
 * http://soygus.com
 * @author @MadeByGus
 * @version 1.1.0
 * Copyright 2015. MIT licensed.
 */
(function() {
    "use-strict";
    $(document).ready(function() {
        $(".disable-anchors a").click(function(a) {
            a.preventDefault();
        }), $("[data-href]").click(function() {
            var a, b, c, d;
            b = window.location.pathname.split("/"), a = b[b.length - 1], c = b[b.length - 2], 
            d = {}, d[c] = a, window.history.pushState(d, "", a), document.location.replace($(this).data("href"));
        });
    });
}).call(this);

/*BAR PROMO TOP FUNCTION*/
$( "#limited" ).click(function() {
  $( "#promo" ).slideToggle( 400 )
  });

  $(document).ready(function() {
      $(window).bind('scroll', function() {
          var headerHeight = $(window).height() - 70;
          if ($(window).scrollTop() > headerHeight) {
              $('header').addClass('fixed');
              }
          else {
              $('header').removeClass('fixed');
              }
      });
});

/*BUTTON SEARCH*/
function enableordisable(){
  if($("#search_bar").val() !== ""){
    $("#search_button").removeAttr("disabled");
  } else{
    $("#search_button").attr("disabled", "disabled");
  }
} 
// $("#search_bar").change(enableordisable());
// $("#search_bar").focus(enableordisable()).keyup(enableordisable()).keydown(enableordisable()).keypress(enableordisable());
$("#search_bar").keypress(enableordisable);
// $("#search_bar").change(function(){
// enableordisable();
// });

/*SCROLL MENU*/  
$(document).scroll(function() {    
  var scroll = $(this).scrollTop();
  if (scroll >= 500) {
      $(".header-scroll").addClass("active");
  } else {
      $(".header-scroll").removeClass("active");
  }
});

/*COLLAPSE */
$('.collapse').on('click',function(e){
  e.preventDefault();
  $(this).toggleClass('active');
});
$('.ver-mas-areas-academicas').on('click',function(e){
  e.preventDefault();
  $(this).toggleClass('active');
});
$('.collapse-home').on('click',function(e){
  e.preventDefault();
  $(this).toggleClass('active');
});
/*COLLAPSE CHANGE BUTTON*/
$(".collapse").click(function(){
  $(this).find('.up').toggleClass("hide");  
  $(this).find('.down').toggleClass("hide"); 
  $(this).find('.ver-mas-areas-academicas').toggleClass("hide");  
});
$(".collapse-home").click(function(){
  $(this).find('.up-home').toggleClass("hide");  
  $(this).find('.down-home').toggleClass("hide"); 
});


/*MODAL FORMULARIO*/
$('.btn-form-prginh').click(function(){
    $('.modal-bg').fadeIn();
    $('#modal').fadeIn();
});
$('#close').click(function(){
    $('.modal-bg').fadeOut();   
    $('#modal').fadeOut();
  return false;
});

/*Menú Toggle*/
$(".switch").click(function() {
  $(this).toggleClass("on");
  $("#overlay-menu").toggleClass("open");
  $(".text-menu").toggleClass("hidde");
});

/*ACTIVE MENU*/
